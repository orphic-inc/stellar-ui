node_modules/
dist/
.playwright-mcp/
e2e/.auth/
playwright-report/
test-results/
.env
.DS_Store

# Photoshop sources — keep local, not in git (LFS TBD)
*.psd

# Handoff/scrap docs stay out of the committed tree. docs/ is durable-only
# (adr/, agents/, PRDs). Persistent home for handoffs: ~/.claude/projects/.../handoff/.
docs/*handoff*.md
docs/handoff/

{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": [
    "platformAutomerge is off deliberately: GitHub native auto-merge ignores the branch-protection app-bypass, so Renovate merges its own automerge PRs directly as the app (the bypass on `main` covers the Renovate app). Required human review stays for everything else; the `test` required check still gates every merge.",
    "schedule:weekends rather than schedule:weekly: weekly resolves to schedule:earlyMondays, which is the cron `* 0-3 * * 1` — a four-hour window, once a week. Renovate only opens PRs if a run happens to land inside it, and on a repo this quiet the runs do not. Weekends (`* * * * 0,6`) is a 48-hour window and still keeps PR noise off working hours; prConcurrentLimit caps the batch."
  ],
  "extends": [
    "config:recommended",
    ":dependencyDashboard",
    "schedule:weekends"
  ],
  "prConcurrentLimit": 5,
  "platformAutomerge": false,
  "packageRules": [
    {
      "description": "Dev tooling: safe to automerge patch/minor",
      "matchDepTypes": ["devDependencies"],
      "matchUpdateTypes": ["patch", "minor"],
      "automerge": true
    },
    {
      "description": "Group non-major npm deps to keep PR volume low",
      "matchManagers": ["npm"],
      "matchUpdateTypes": ["minor", "patch"],
      "groupName": "npm non-major"
    },
    {
      "description": "Pin GitHub Action digests fresh; safe to automerge (clears the Node20-deprecated actions/checkout + actions/setup-node SHAs)",
      "matchManagers": ["github-actions"],
      "automerge": true,
      "groupName": "github-actions"
    },
    {
      "description": "Base images: review every bump (ADR-0001 / api#99 class) — node + nginx",
      "matchDatasources": ["docker"],
      "automerge": false,
      "groupName": null
    },
    {
      "description": "Major bumps always reviewed",
      "matchUpdateTypes": ["major"],
      "automerge": false
    }
  ],
  "lockFileMaintenance": { "enabled": true, "automerge": true }
}

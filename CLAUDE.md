# CLAUDE.md — stellar-ui

The guidance for this repo lives in **[AGENTS.md](AGENTS.md)**, which is the single source of truth for every agent that works here — commands, environment, the architecture map, the theming contract, RTK Query patterns, the permission catalogue, commit workflow, and testing.

This file exists only so Claude Code picks that up automatically. It is imported below; do not duplicate content here, or the two copies will drift apart (they did once, and reconciling them is what produced this split).

Matches the same split in [stellar-api](https://github.com/orphic-inc/stellar-api), so the maintained guide is `AGENTS.md` in both repos.

@AGENTS.md
